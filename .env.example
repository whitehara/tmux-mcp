IMAGE_TAG=latest

# Port to expose the MCP server on the host
TMUX_MCP_PORT=8090

# UID/GID of the host user running tmux (run: id -u / id -g)
HOST_UID=1000
HOST_GID=1000

# tmux-mcp server settings
TMUX_MCP_MAX_COMMANDS=1000
TMUX_MCP_COMMAND_TTL=600
TMUX_MCP_SHELL=bash
